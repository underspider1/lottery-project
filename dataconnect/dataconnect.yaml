specVersion: "v1beta"
serviceId: "lottery_project"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "lottery_project-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
